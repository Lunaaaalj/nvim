-- Warm brown/cream/muted-green, cozy minimalist. Dark + light variants.
-- Melange has no built-in transparency option; the global ColorScheme autocmd
-- in lua/defaults/init.lua strips the background. `:colorscheme melange`.
return {
    "savq/melange-nvim",
    lazy = false,
    priority = 1000,
}
